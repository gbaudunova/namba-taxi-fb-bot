# from .need_parameters import need_phone
# from .need_parameters import need_address
#
#
# def decide_message(sender_id, message_text, data):
#     if message_text == '+996':
#         need_phone(sender_id, data)
#     elif len(message_text) > 3 or len(message_text) < 150:
#         need_address(sender_id, data)
#     else:
#         print('!!!!!!!!!!!')